#include <iostream>
#include <cstring>

int main()
{
    std::string str;
    int arr[27];
    std::cin >> str;
    for (int i = 0; i < 27; i++){
        std::cin >> arr[i];
    }
    int cost {};
    std::string output;
    for (int i = 0; i < str.length(); i++){
        if (str[i] == '?') {
            
        } 
    }
    return 0;
}