#include <iostream>

int main()
{
    int n, k, q;
    std::cin >> n >> k >> q;
    while (n--){
        int l, r;
        std::cin >> l >> r;
    }
    while (q--)
    {
        int a, b;
        std::cin >> a >> b;
    }

    return 0;
}